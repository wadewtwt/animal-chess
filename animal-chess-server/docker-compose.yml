version: '3.8'

services:
  animal-chess-server:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: animal-chess-server
    ports:
      - "8083:8083"
    restart: always
    environment:
      - TZ=Asia/Shanghai
